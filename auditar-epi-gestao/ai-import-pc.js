(() => {
  const ENDPOINT='https://script.google.com/macros/s/AKfycbxNG-wU-jZMKMR2cb1nR9OUd31GSUpGM0FIEagZEUP7sAHxkahLDuJ6T3wZvEe9rm6WrQ/exec';
  const KEY='gestaoEpiAiActivationKeyV1';
  const $=(s,r=document)=>r.querySelector(s);
  const clean=v=>String(v??'').replace(/\s+/g,' ').trim();
  const norm=v=>clean(v).normalize('NFD').replace(/[\u0300-\u036f]/g,'').toLowerCase();
  let file=null;
  function toast(msg){const e=$('#toast');if(!e)return alert(msg);e.textContent=msg;e.classList.add('show');setTimeout(()=>e.classList.remove('show'),3000);}
  function dataUrl(f){return new Promise((res,rej)=>{const r=new FileReader();r.onload=()=>res(String(r.result||''));r.onerror=()=>rej(new Error('Falha ao ler arquivo.'));r.readAsDataURL(f);});}
  async function pdfLines(f){if(!window.pdfjsLib)return[];try{pdfjsLib.GlobalWorkerOptions.workerSrc='https://cdnjs.cloudflare.com/ajax/libs/pdf.js/3.11.174/pdf.worker.min.js';const pdf=await pdfjsLib.getDocument({data:new Uint8Array(await f.arrayBuffer())}).promise,lines=[];for(let p=1;p<=pdf.numPages;p++){const pg=await pdf.getPage(p),tc=await pg.getTextContent(),g=new Map();tc.items.forEach(i=>{const y=Math.round(i.transform?.[5]||0),x=i.transform?.[4]||0,key=[...g.keys()].find(k=>Math.abs(k-y)<=2)??y;if(!g.has(key))g.set(key,[]);g.get(key).push({x,t:clean(i.str)});});[...g.entries()].sort((a,b)=>b[0]-a[0]).forEach(([,a])=>{const l=a.sort((x,y)=>x.x-y.x).map(z=>z.t).filter(Boolean).join(' ');if(l)lines.push(l);});}return lines;}catch(_){return[];}}
  function enrich(w,lines){const l=lines.find(x=>norm(x).includes(norm(w.name)));if(!l)return w;const cpf=(l.match(/\b\d{3}\.?\d{3}\.?\d{3}[-\s]?\d{2}\b/)||[])[0]||'',m=l.match(/(?:matr[ií]cula|registro|chapa)\s*[:#-]?\s*([A-Za-z0-9.-]+)/i);return{...w,cpf:w.cpf||cpf,reg:w.reg||(m?.[1]||'')};}
  function csv(v){const s=String(v??'');return /[;"\n]/.test(s)?`"${s.replace(/"/g,'""')}"`:s;}
  function send(rows){const text=['Nome;CPF;Matrícula;Cargo;Setor',...rows.map(w=>[w.name,w.cpf||'',w.reg||'',w.role||'',w.sector||''].map(csv).join(';'))].join('\n'),blob=new Blob([text],{type:'text/csv'}),f=new File([blob],'lista-organizada-ia.csv',{type:'text/csv'}),dt=new DataTransfer();dt.items.add(f);const input=$('#pcImportFile');input.files=dt.files;input.dispatchEvent(new Event('change',{bubbles:true}));}
  function inject(){const panel=$('#pcImport .panel');if(!panel||$('#pcAiImportBox'))return;const box=document.createElement('div');box.id='pcAiImportBox';box.style.cssText='margin-top:16px;padding:14px;border:1px solid #d7e7e4;border-radius:14px;background:#f7fbfa';box.innerHTML=`<div style="display:flex;gap:10px;justify-content:space-between;align-items:center;flex-wrap:wrap"><div><b>✨ IA para PDF difícil</b><br><small>Use quando a leitura normal do PDF ficar bagunçada ou incompleta.</small></div><button id="pcRunAiImport" class="secondary" type="button">✨ Organizar com IA</button></div><div id="pcAiStatus" class="pc-small" style="margin-top:8px"></div><div id="pcAiConfig" style="display:none;margin-top:10px"><label style="display:grid;gap:5px;font-size:12px;font-weight:850">Chave de ativação da IA<input id="pcAiKey" type="password" style="min-height:44px;border:1px solid #cfdfdc;border-radius:10px;padding:0 10px"></label><button id="pcSaveAiKey" class="primary" type="button" style="margin-top:8px">Salvar neste computador</button></div>`;panel.appendChild(box);$('#pcRunAiImport').onclick=run;$('#pcSaveAiKey').onclick=()=>{const v=clean($('#pcAiKey').value);if(!v)return toast('Informe a chave de ativação.');localStorage.setItem(KEY,v);$('#pcAiConfig').style.display='none';$('#pcAiStatus').textContent='IA ativada neste computador.';};$('#pcImportFile')?.addEventListener('change',e=>{file=e.target.files?.[0]||null;});}
  async function run(){if(!file)return toast('Selecione primeiro o PDF.');if((file.name.split('.').pop()||'').toLowerCase()!=='pdf')return toast('A IA é indicada para PDF; Excel e CSV já são lidos diretamente.');const key=clean(localStorage.getItem(KEY)||'');if(!key){$('#pcAiConfig').style.display='block';$('#pcAiStatus').textContent='Informe a chave de ativação apenas neste computador.';return;}const btn=$('#pcRunAiImport');btn.disabled=true;btn.textContent='✨ Lendo com IA…';try{const doc=await dataUrl(file);if(doc.length>18000000)throw new Error('PDF muito grande para análise por IA.');const res=await fetch(ENDPOINT,{method:'POST',headers:{'Content-Type':'text/plain;charset=utf-8'},body:JSON.stringify({action:'ai_assistant',syncKey:key,payload:{mode:'employee_pdf_import',document:doc}})}),json=await res.json();if(!json?.ok)throw new Error(json?.message||'A IA não conseguiu ler a lista.');const employees=Array.isArray(json.result?.employees)?json.result.employees:[];if(!employees.length)throw new Error('Nenhum funcionário identificado pela IA.');const lines=await pdfLines(file),rows=employees.map(e=>({name:clean(e.name),cpf:clean(e.cpf),reg:clean(e.reg),role:clean(e.role),sector:clean(e.sector)})).filter(e=>e.name).map(e=>enrich(e,lines));send(rows);$('#pcAiStatus').textContent=`IA organizou ${rows.length} trabalhador(es). Confira a prévia antes de cadastrar.`;toast('Lista organizada com IA.');}catch(e){$('#pcAiStatus').textContent=String(e.message||e);toast(e.message||'Falha ao usar IA.');}finally{btn.disabled=false;btn.textContent='✨ Organizar com IA';}}
  function init(){inject();const target=$('#pcImport');if(target)new MutationObserver(inject).observe(target,{childList:true,subtree:true});}
  if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',()=>setTimeout(init,150),{once:true});else setTimeout(init,150);
})();