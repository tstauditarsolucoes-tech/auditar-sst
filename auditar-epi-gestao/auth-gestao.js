(() => {
  /* A autenticação da versão PC agora é inicializada diretamente por
     central-config.js. Este arquivo é mantido apenas por compatibilidade com
     versões e pacotes antigos que ainda o referenciam. */
  if(window.__GESTAO_AUTH_CORE_ACTIVE)return;
})();
