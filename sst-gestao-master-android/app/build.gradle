plugins {
    id 'com.android.application'
}

android {
    namespace 'br.com.sstgestao.master'
    compileSdk 35

    defaultConfig {
        applicationId 'br.com.sstgestao.master'
        minSdk 23
        targetSdk 35
        versionCode 1
        versionName '1.0.0'
    }

    buildTypes {
        release {
            minifyEnabled false
            signingConfig signingConfigs.debug
        }
    }
}

dependencies {
}
